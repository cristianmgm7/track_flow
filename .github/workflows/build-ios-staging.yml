name: 🍎 Build iOS Staging → TestFlight

on:
  push:
    branches: [staging]
  workflow_dispatch:

jobs:
  build-ios-staging:
    name: 🍎 iOS Staging (TestFlight Internal)
    runs-on: macos-14

    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4
        with:
          submodules: false

      - name: 🎯 Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: "3.29.3"
          channel: "stable"
          cache: true

      - name: 📦 Install dependencies
        run: flutter pub get

      - name: 🔧 Generate code
        run: flutter packages pub run build_runner build --delete-conflicting-outputs

      - name: 🔧 Configure Flutter
        run: |
          flutter config --no-analytics
          flutter config --no-cli-animations

      - name: 🔧 Import signing certificates
        uses: apple-actions/import-codesign-certs@v3
        with:
          p12-file-base64: ${{ secrets.IOS_CERTIFICATE_BASE64 }}
          p12-password: ${{ secrets.IOS_CERTIFICATE_PASSWORD }}

      - name: 🔧 Download provisioning profiles (Staging)
        uses: apple-actions/download-provisioning-profiles@v3
        with:
          bundle-id: com.trackflow.staging
          issuer-id: ${{ secrets.APPLE_ISSUER_ID }}
          api-key-id: ${{ secrets.APPLE_API_KEY_ID }}
          api-private-key: ${{ secrets.APPLE_API_PRIVATE_KEY }}
          profile-type: IOS_APP_STORE

      - name: 🔨 Build IPA (staging)
        run: |
          echo "🚀 Building iOS staging ipa..."
          export FLUTTER_XCODE_ARGS="-allowProvisioningUpdates"
          flutter build ipa --flavor staging --release --verbose --export-options-plist=ios/ExportOptions.plist

      - name: 🧭 Locate IPA
        run: |
          set -e
          IPA=$(ls -1 build/ios/ipa/*.ipa | head -n1)
          echo "IPA_PATH=$IPA" >> $GITHUB_ENV
          echo "Found IPA at: $IPA"

      - name: ✅ Verify IPA
        run: |
          if [ -f "$IPA_PATH" ]; then
            echo "✅ IPA generated: $IPA_PATH"
            echo "📏 Size: $(du -h "$IPA_PATH" | cut -f1)"
          else
            echo "❌ IPA not found"
            ls -la build/ios/ipa/
            exit 1
          fi

      - name: 🍎 Upload to TestFlight (Staging)
        uses: apple-actions/upload-testflight-build@v1
        with:
          app-path: ${{ env.IPA_PATH }}
          issuer-id: ${{ secrets.APPLE_ISSUER_ID }}
          api-key-id: ${{ secrets.APPLE_API_KEY_ID }}
          api-private-key: ${{ secrets.APPLE_API_PRIVATE_KEY }}
